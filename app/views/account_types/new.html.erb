<section class="form-page">
  <header class="form-header">
    <h1><%= t('ui.actions.new') %> <%= t('activerecord.models.account_type.one') %></h1>
    <p>Defina um novo tipo de conta para classificar suas contas bancárias</p>
  </header>

  <%= render 'form', account_type: @account_type %>
</section>