<div>
 <div>
 <h1>✨ <%= t('ui.actions.new') %> <%= t('activerecord.models.transaction.one') %></h1>
 <p><%= t('ui.transaction.new.subtitle') %></p>
 </div>
 
 <%= link_to transactions_path do %>
 <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
 <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
 </svg>
 <%= t('ui.actions.back') %>
 <% end %>
</div>

<!-- Progress indicator -->
<div>
 <div>
 <div>Tipo</div>
 <div>Detalhes</div>
 <div>Contas</div>
 <div>Finalizar</div>
 </div>
</div>

<div>
 <%= render 'form', transaction: @transaction %>
</div>