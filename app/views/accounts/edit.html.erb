<section class="form-page">
  <header class="form-header">
    <h1>Editar Conta</h1>
    <p>Atualize as informações da conta <%= @account.name %></p>
  </header>

  <%= render 'form', account: @account %>
</section>