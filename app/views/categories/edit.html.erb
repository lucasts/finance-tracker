<section class="form-page">
  <header class="form-header">
    <h1>Editar Categoria</h1>
    <p>Atualize as informações da categoria <%= @category.name %></p>
  </header>

  <%= render 'form', category: @category %>
</section>